mysql -u root -pCREATE DATABASE weather;USE weather;CREATE TABLE exported_weather (stationId VARCHAR(12), date DATE, tmin INT, tmax INT, snow INT, snwd INT, prcp INT);CREATE TABLE staging_weather (stationId VARCHAR(12), date DATE, tmin INT, tmax INT, snow INT, snwd INT, prcp INT);sqoop export \--connect jdbc:mysql://localhost/weather \--connection-manager org.apache.sqoop.manager.MySQLManager \--table exported_weather \--staging-table staging_weather \--clear-staging-table \--export-dir /user/maria_dev/sqoop_input/ \--options-file ~/.access_credentials \--num-mappers 4 \--input-fields-terminated-by ','SELECT count(*) FROM exported_weather;SELECT * FROM exported_weather ORDER BY stationId, date LIMIT 10;